use std::str::FromStr;

use proc_macro2::TokenStream;
use proc_macros_impl::*;

fn main() {
    //let code = r#" "bob" "#;
    //let input = TokenStream::from_str(code).expect("tokenize");
    //let output = handle_impl(input);
    //println!("{output}");
}
