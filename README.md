## Arch

### gamecore
game incl Ui
tick(inputs) -> outputs
outputs:
  * scenegraph
  * sounds
  * paths to reflect
queryable state
path-based reflection
serializeable state
like a cpu emulator

### webshell
driver (ticks)
inspects via refl
draws scenegraph

## Run

cd webui
trunk serve